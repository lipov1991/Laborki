package pl.lipov.laborki.presentation.map

enum class MarkerCategory(
    private val value: String
) {

    market(value = "Market"),
    restauracja(value = "Restauracja"),
    bank(value = "Bank")
}